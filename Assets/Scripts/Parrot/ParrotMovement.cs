using UnityEngine;

public class ParrotMovement : EnemyAbs
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }
    //help im being held hostage 
    //yoav is a murderer
    // Update is called once per frame
    void Update()
    {
        
    }
}
